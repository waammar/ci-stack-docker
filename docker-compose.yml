version: '2'

services:
  # postgresql:
  #   restart: always
  #   image: sameersbn/postgresql:9.4-21
  #   env_file: config
  #   volumes:
  #     - /srv/docker/postgresql:/var/lib/postgresql

  # gitlab:
  #   restart: always
  #   image: sameersbn/gitlab:8.7.5
  #   env_file: config
  #   links:
  #     - redis:redis
  #     - postgresql:postgresql
  #   ports:
  #     - "10080:80"
  #     - "10022:22"
  #   depends_on:
  #     - postgresql
  #     - redis
  #   volumes:
  #     - /srv/docker/gitlab:/home/git/data

  # redis:
  #   restart: always
  #   image: sameersbn/redis:latest
  #   env_file: config
  #   volumes:
  #     - /srv/docker/redis:/var/lib/redis

  jenkins:
    build: ./jenkins
    env_file: config
    ports:
      - "18080:8080"
    volumes:
      - /srv/docker/jenkins_home:/var/jenkins_home